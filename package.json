{
  "name": "sua-api",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "npx json-server data.json --port $PORT --static ./data.json"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "json-server": "^1.0.0-beta.15"
  }
}